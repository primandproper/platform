-- Code generated by github.com/primandproper/platform-go/v13/saga/internal/queriesgen. DO NOT EDIT.

-- name: GetSagaInstance :one
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until
FROM saga_instances
WHERE saga_instances.archived_at IS NULL
	AND saga_instances.id = sqlc.arg(id);

-- name: ListSagaInstances :many
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	) AS filtered_count,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	) AS total_count
FROM saga_instances
WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
	AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	AND saga_instances.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY saga_instances.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListSagaInstancesDescending :many
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	) AS filtered_count,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	) AS total_count
FROM saga_instances
WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
	AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	AND (saga_instances.id <= COALESCE(sqlc.narg(page_cursor), saga_instances.id) AND saga_instances.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY saga_instances.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListSagaInstancesByDefinition :many
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
			AND saga_instances.definition = sqlc.arg(definition)
	) AS filtered_count,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
			AND saga_instances.definition = sqlc.arg(definition)
	) AS total_count
FROM saga_instances
WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
	AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	AND saga_instances.definition = sqlc.arg(definition)
	AND saga_instances.id > COALESCE(sqlc.narg(page_cursor), '')
ORDER BY saga_instances.id ASC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ListSagaInstancesByDefinitionDescending :many
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
			)
			AND (
				saga_instances.last_updated_at IS NULL
				OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
			)
			AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
			AND saga_instances.definition = sqlc.arg(definition)
	) AS filtered_count,
	(
		SELECT COUNT(saga_instances.id)
		FROM saga_instances
		WHERE (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
			AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
			AND saga_instances.definition = sqlc.arg(definition)
	) AS total_count
FROM saga_instances
WHERE saga_instances.created_at > COALESCE(sqlc.narg(created_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	AND saga_instances.created_at < COALESCE(sqlc.narg(created_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at > COALESCE(sqlc.narg(updated_after), (SELECT CURRENT_TIMESTAMP - '999 years'::INTERVAL))
	)
	AND (
		saga_instances.last_updated_at IS NULL
		OR saga_instances.last_updated_at < COALESCE(sqlc.narg(updated_before), (SELECT CURRENT_TIMESTAMP + '999 years'::INTERVAL))
	)
	AND (COALESCE(sqlc.narg(include_archived), false)::boolean OR saga_instances.archived_at IS NULL)
	AND saga_instances.status IN (sqlc.arg(status_1), sqlc.arg(status_2), sqlc.arg(status_3), sqlc.arg(status_4), sqlc.arg(status_5))
	AND saga_instances.definition = sqlc.arg(definition)
	AND (saga_instances.id <= COALESCE(sqlc.narg(page_cursor), saga_instances.id) AND saga_instances.id <> COALESCE(sqlc.narg(page_cursor), ''))
ORDER BY saga_instances.id DESC
LIMIT COALESCE(sqlc.narg(result_limit), 50);

-- name: ClaimableSagaInstanceIDs :many
SELECT saga_instances.id
FROM saga_instances
WHERE saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status))
	AND saga_instances.next_attempt <= sqlc.arg(due_at)
	AND (saga_instances.claimed_until IS NULL OR saga_instances.claimed_until <= sqlc.arg(lease_expired_by))
ORDER BY saga_instances.next_attempt, saga_instances.created_at, saga_instances.id
LIMIT COALESCE(sqlc.narg(result_limit), 50)
FOR UPDATE SKIP LOCKED;

-- name: ListSagaInstancesByIDs :many
SELECT
	saga_instances.id,
	saga_instances.definition,
	saga_instances.status,
	saga_instances.current_step,
	saga_instances.step_names,
	saga_instances.state,
	saga_instances.attempts,
	saga_instances.last_error,
	saga_instances.resume_status,
	saga_instances.created_at,
	saga_instances.last_updated_at,
	saga_instances.archived_at,
	saga_instances.next_attempt,
	saga_instances.claimed_until
FROM saga_instances
WHERE saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status))
	AND saga_instances.id = ANY(sqlc.arg(ids)::text[])
ORDER BY saga_instances.next_attempt, saga_instances.created_at, saga_instances.id;

-- name: InsertSagaInstance :exec
INSERT INTO saga_instances (
	id,
	definition,
	status,
	current_step,
	step_names,
	state,
	attempts,
	last_error,
	resume_status,
	created_at,
	next_attempt
) VALUES (
	sqlc.arg(id),
	sqlc.arg(definition),
	sqlc.arg(status),
	sqlc.arg(current_step),
	sqlc.arg(step_names),
	sqlc.narg(state),
	sqlc.arg(attempts),
	sqlc.arg(last_error),
	sqlc.arg(resume_status),
	sqlc.arg(created_at),
	sqlc.arg(next_attempt)
);

-- name: ClaimSagaInstances :execrows
UPDATE saga_instances SET
	claimed_until = sqlc.arg(claimed_until),
	last_updated_at = sqlc.arg(last_updated_at),
	attempts = attempts + 1
WHERE saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status))
	AND saga_instances.id = ANY(sqlc.arg(ids)::text[]);

-- name: AdvanceSagaInstance :execrows
UPDATE saga_instances SET
	status = sqlc.arg(status),
	current_step = sqlc.arg(current_step),
	state = sqlc.narg(state),
	last_error = sqlc.arg(last_error),
	resume_status = sqlc.arg(resume_status),
	next_attempt = sqlc.arg(next_attempt),
	last_updated_at = sqlc.arg(last_updated_at),
	attempts = 0
WHERE saga_instances.id = sqlc.arg(id)
	AND saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status));

-- name: AdvanceSagaInstanceAndClearLease :execrows
UPDATE saga_instances SET
	status = sqlc.arg(status),
	current_step = sqlc.arg(current_step),
	state = sqlc.narg(state),
	last_error = sqlc.arg(last_error),
	resume_status = sqlc.arg(resume_status),
	next_attempt = sqlc.arg(next_attempt),
	last_updated_at = sqlc.arg(last_updated_at),
	attempts = 0,
	claimed_until = NULL
WHERE saga_instances.id = sqlc.arg(id)
	AND saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status));

-- name: RescheduleSagaInstance :execrows
UPDATE saga_instances SET
	attempts = sqlc.arg(attempts),
	next_attempt = sqlc.arg(next_attempt),
	last_error = sqlc.arg(last_error),
	last_updated_at = sqlc.arg(last_updated_at),
	claimed_until = NULL
WHERE saga_instances.id = sqlc.arg(id)
	AND saga_instances.status IN (sqlc.arg(running_status), sqlc.arg(compensating_status));

-- name: ReleaseSagaInstance :execrows
UPDATE saga_instances SET
	claimed_until = NULL,
	last_updated_at = sqlc.arg(last_updated_at)
WHERE saga_instances.id = sqlc.arg(id);

-- name: RequeueSagaInstance :execrows
UPDATE saga_instances SET
	status = sqlc.arg(status),
	next_attempt = sqlc.arg(next_attempt),
	last_updated_at = sqlc.arg(last_updated_at),
	resume_status = '',
	attempts = 0,
	claimed_until = NULL
WHERE saga_instances.id = sqlc.arg(id)
	AND saga_instances.status = ANY(sqlc.arg(from_statuses)::text[]);
