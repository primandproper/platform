// Code generated by github.com/primandproper/sqlc-gen-unison. DO NOT EDIT.
//
// sqlc:   v1.31.1
// unison: v0.3.0

package sagadb

import (
	"context"
)

// Querier is every query in this package, with one signature per query
// regardless of which dialect answers it.
//
// That is the point of this package: the params type, the row type, and the
// method signature are the same on every dialect, so nothing above this line
// knows or cares which database is underneath.
//
// # A note on the :execrows count
//
// MySQL reports rows *changed*: an UPDATE that sets a column to the value it
// already held affects zero rows there. Postgres and SQLite report rows
// *matched*, and count that same UPDATE as one.
//
// So a statement that gates on the count — treating zero as "not found" — is
// correct on two engines and wrong on the third. Either give it a predicate
// that discriminates, or set clientFoundRows=true in the MySQL DSN, which
// switches MySQL to matched semantics.
//
// # A note on empty lists
//
// A list parameter that is empty matches nothing, on every dialect: Postgres
// binds an empty array to `= ANY`, and the other two expand to `IN (NULL)` because
// `IN ()` is a syntax error there. Asking for the rows whose key is in an empty
// set gets no rows back, which is what the empty set says, so a caller does not
// have to guard the call.
//
// The negation is where they part company, and nothing below can warn you. An
// empty list makes `NOT IN (NULL)` never true, so it matches nothing, while
// Postgres's empty `<> ALL` is true and matches everything. Both readings are
// defensible and no shared signature can say which was meant — so test
// membership rather than its negation, and let the caller decide what an empty
// set means before it calls.
type Querier interface {
	// AdvanceSagaInstance runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	AdvanceSagaInstance(ctx context.Context, db DBTX, arg AdvanceSagaInstanceParams) (int64, error)
	// AdvanceSagaInstanceAndClearLease runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	AdvanceSagaInstanceAndClearLease(ctx context.Context, db DBTX, arg AdvanceSagaInstanceAndClearLeaseParams) (int64, error)
	// ClaimSagaInstances runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ClaimSagaInstances(ctx context.Context, db DBTX, arg ClaimSagaInstancesParams) (int64, error)
	// ClaimableSagaInstanceIDs runs the :many query.
	ClaimableSagaInstanceIDs(ctx context.Context, db DBTX, arg ClaimableSagaInstanceIDsParams) ([]ClaimableSagaInstanceIDsRow, error)
	// GetSagaInstance runs the :one query.
	GetSagaInstance(ctx context.Context, db DBTX, arg GetSagaInstanceParams) (GetSagaInstanceRow, error)
	// InsertSagaInstance runs the :exec query.
	InsertSagaInstance(ctx context.Context, db DBTX, arg InsertSagaInstanceParams) error
	// ListSagaInstances runs the :many query.
	ListSagaInstances(ctx context.Context, db DBTX, arg ListSagaInstancesParams) ([]ListSagaInstancesRow, error)
	// ListSagaInstancesByDefinition runs the :many query.
	ListSagaInstancesByDefinition(ctx context.Context, db DBTX, arg ListSagaInstancesByDefinitionParams) ([]ListSagaInstancesByDefinitionRow, error)
	// ListSagaInstancesByDefinitionDescending runs the :many query.
	ListSagaInstancesByDefinitionDescending(ctx context.Context, db DBTX, arg ListSagaInstancesByDefinitionDescendingParams) ([]ListSagaInstancesByDefinitionDescendingRow, error)
	// ListSagaInstancesByIDs runs the :many query.
	ListSagaInstancesByIDs(ctx context.Context, db DBTX, arg ListSagaInstancesByIDsParams) ([]ListSagaInstancesByIDsRow, error)
	// ListSagaInstancesDescending runs the :many query.
	ListSagaInstancesDescending(ctx context.Context, db DBTX, arg ListSagaInstancesDescendingParams) ([]ListSagaInstancesDescendingRow, error)
	// ReleaseSagaInstance runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	ReleaseSagaInstance(ctx context.Context, db DBTX, arg ReleaseSagaInstanceParams) (int64, error)
	// RequeueSagaInstance runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RequeueSagaInstance(ctx context.Context, db DBTX, arg RequeueSagaInstanceParams) (int64, error)
	// RescheduleSagaInstance runs the :execrows query.
	//
	// The count means different things on different engines; see the note
	// on Querier.
	RescheduleSagaInstance(ctx context.Context, db DBTX, arg RescheduleSagaInstanceParams) (int64, error)
}
